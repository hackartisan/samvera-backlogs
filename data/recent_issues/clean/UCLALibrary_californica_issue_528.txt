Re-ingest content with ARK field in object model
ISSUE We've significantly refactored the way we handle ARKs and the cleanest way to update content is via a re-ingest using the refactored code. ACCEPTANCE    All existing production data is re-imported (metadata update only) to reflect new ark property and import rules   Stage   Recreate an empty stage environment  ingest bennett, connell, ladnn    NOTE Moving production to separate ticket  Production   Confirm metadata imported correctly in stage ()  Recreate an empty prod environment ()  ingest bennett, connell, ladnn ()    Blocked ARK related tickets Requires #538