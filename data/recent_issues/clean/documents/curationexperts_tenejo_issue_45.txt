Select and Validate the CSV
STORY As a librarian I would like to use a spreadsheet to specify which works to import. ACCEPTANCE   I can select a CSV to upload  I can click the "Start Import" button to have the CSV validated  The system reports errors in the CSV  The system reports warnings in the CSV  This is at a "secret" path or behind a feature flipper (shouldn't show up to the public in prod yet)  NOTES ERROR CASES  File is not parseable (give the user the line number of the first error if possible) Missing required metadata (e.g. one row is missing a string in the Titled field) Missing required columns (e.g. the CSV is missing a column with the header 'title')  WARNING CASES  Extra columns  NOT AN ERROR  tbd Requried columns can be in any order