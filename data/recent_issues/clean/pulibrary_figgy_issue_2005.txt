`FileMetadata` is not deleted when PDF derivative files are for updates to ScannedResources
Discovered when resolving #1994 , and relates to the following block in the code base:     def run       return unless resource.is_a?(ScannedResource) && pdf_file       return if change_set.changed.except("file_metadata").empty?       CleanupFilesJob.perform_later(file_identifiers: [pdf_file.file_identifiers.first.to_s])       resource.file_metadata.delete(pdf_file)     end  (