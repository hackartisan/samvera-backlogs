Update feature specs to fill out and verify all work fields
Descriptive summary We should update our feature specs for each work type (spec/features/create_article_spec.rb, spec/features/create_dataset_spec.rb, etc.) so that they put content in and then verify every field on the work form.  Currently, those specs only fill out the required fields. This will let us know if any bugs pop up that prevent individual fields from saving properly.  fill_in every field on the work form Save the work Edit the work and verify that each field contains the test data that was entered in step 1.