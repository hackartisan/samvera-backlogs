update mappers
more updates now that we've done some real-world batch ingests  adds edtf and edtf-humanize dependencies  magazine  adds current digital.lafayette.edu URLs to identifiers (with url: prefix)  newspaper  uses newly remediated keys for date_issued, date_uploaded, and url adds url: prefix to ContentDM URLs adds current digital.lafayette.edu URLs to identifiers (with url: prefix) adds lafayette: prefix to legacy Fedora PIDs adds date to title  The Lafayette becomes The Lafayette - August 14, 2019 (or whatever the date_issued is)    shakespeare bulletin  adds current digital.lafayette.edu URLs to identifiers (with url: prefix) adds file prefix to identifiers (with lafayette: prefix)