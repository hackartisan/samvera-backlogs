Allow background processing in development env
Switches ActiveJob queue adapter to :sidekiq by default. Switches database fro SQLite3 to MySQL to handle multiple connections better. Makes jobs threadsafe with eager loading turned off, by explicitly requiring files that batch ingest jobs depend on. These are in an initializer. Adds a lambda function used for determining when a NOID identifier is in use by checking to see if it exists in Fedora anywhere.  Also...  Removes Spring from development. It's suspected of causing issues when running concurrent jobs in development. This may be a false negative, but we're taking it out for now until we have time to debug it further.