handle s3 credentials for dev and test in app; automatically setup an…
…d teardown fixtures for tests so with these changes we don't have to run a bunch of the tasks that are in master branch's README. You just need to have minio running in docker and you can run rake s3:setup to create a bucket and populate it and rake s3:teardown to clean it out. The tests now also create their own bucket and populate it before the test suite is run too