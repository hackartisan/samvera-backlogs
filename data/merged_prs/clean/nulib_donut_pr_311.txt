alias capture_device to model, make it visible in the ui
fixes  capture_device already exists in hydra works, so instead of creating another one, i've aliased model to capture_device and made model visible in the UI