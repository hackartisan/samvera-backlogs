fix small issues
This PR fixes small issues seen in face 2 face meeting at Indiana University. Some where fixed during meeting so bunching up everything in single PR.  Asset form (AMS2-379) Where asset can not be created without contribution Fixes Edit form of a child work to enforce title field Delete child work DigitalInstantiation (EssenceTrack) when editing Add child_create_button attribute/module to class, to allow control of showing child create buttons on forms Hide child create button on EssenceTrack and DigitalInstantiation Redo the enforcing of title (force check for parent object in actor, when creating/editing a work)