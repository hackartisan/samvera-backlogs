fix solr >=7.2.0 bug in identifier_controller
fixes a bug we were encountering where the IdentifierController spec was failing because we weren't specifying the lucene parser when querying the identifiers. see: 