Maps pbcoreAssetDate elements to Asset model
Adds the mapping to the pbcore xml mapper object. Adds new factory for creating PBCore::AssetType objects. Updates pbcore gem (the PBCore::AssetType did not have a :type attribute). Adds PBCore::AssetDate instances to the PBCore::DescriptionDocument factory's :full_aapb trait.  Also,  Updates pbcore gem. Adds module AMS::AAPB to be a home for methods used to interact with AAPB website. Adds some explanatory comments to AMS::IdentifierService. Removes explicit ID assignment from factories; not needed. Updates db/schema.test.rb to reflect schema changes. Fixes deprecation warning from Faker.