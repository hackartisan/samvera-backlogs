collections updates
try a CollectionBrandingInfo (role = "logo") as the thumbnail for a Collection implement slugs?   index identifiers with slug: prefix under collection_slug_ssi field  only allow one slug per collection (+ match /^[a-z0-9\-]+$/ regex)  add behavior to CollectionsController  test behavior on CollectionsController  add slug field to collection form  modify how urls are drawn to prefer collections to use their slugs (do we want to make slugs required?)    modify CollectionsController#show view:  display description / metadata related links    if these wind-up not taking too much effort, we can also try to fix searching across nested collections...  closes #167 closes #270