Delete resources via confirmation process
Description Route delete requests to a specific controller that confirms the user's selection and then deletes the item. Here's a screencast that shows the whole thing in action:  Fixes: #449 and #450 Changes Refactors our controllers because deletion is no longer required. Also reworks the ChangeSetPersister to delete files from works and removes unused code. Are there any major changes in this PR that will change the release process? No Checklist   i18n enabled  adequate test coverage  accessible interface