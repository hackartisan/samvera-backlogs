Only index a collection if its an NUL collection
Just don't index Collections that aren't "NUL Collection/s" at all rather than relying on Glaze to filter them out.   This is instead of this: nulib/next-gen-front-end-react@d2d233f   since staging and prod are named differently....