Fixes location saving
Im still trying to figure out why two fields are rendering. It seems as though it has something to do with  When you remove the as: :controlled_vocabulary completely it only renders one field. Otherwise it renders two.