Version edit form does not display migrated data
Version form from curation concerns (https://github.com/projecthydra/curation_concerns/blob/0f9c1c307aac0641b364159124613aef443e6030/app/controllers/concerns/curation_concerns/file_sets_controller_behavior.rb#L161) Displays the sytem creation date. The code should get the actual version from fedora, not just use the version graph version = curation_concern.original_file.versions.all.map { |v| Hydra::PCDM::File.new(v.uri) CurationConcerns::VersionListPresenter.new(version)  Then the presenter should use the date_created instead of the version graph created for a display value.