Release and Deploy Scholar 4.3.0
Merge remaining PRs  Verify that the hyrax.en.yml, hyrax.es.yml, and hyrax.zh.yml files are in sync  Create a release branch  Deploy to scholar-qa  Run db:seed and turn on registrations  Run through the manual test script  Wipe scholar-qa  Request a security scan  Update the changelog  Create a change request  Attach passing scan result to change request (get approval)  Tag the release  Make any needed change to servers (ruby version, etc.)  Update the .env credentials on the servers & git.uc.edu, if needed  Deploy to scholar.uc.edu  Post-deploy testing  Create a blog post  Mark the change request as successful