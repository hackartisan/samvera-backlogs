Review the search parameters.
Per our audit, it is revealed that we need to take a second look at the search parameters in scholars archive. A validator or service that accepts the input queries and fashions them in a specific way could be used to help this. The two pieces of the query that need to be looked at are the query itself and the sort parameter.
Looks like a query on the Users table, but is not. 